rule concatenation:
    vars:
        L: seq[T1]
        M: seq[T1]
    rewrite:
        L ++ M
        ~>
        L \/ {x |-> y in M | x + max(dom(L)) |-> y}

# rule slice_from_bottom:
#     vars:
#         X: seq[T1]
#         i: int
#     rewrite:
#         X[i:] ~> 0..i <<| X
#
# rule slice_from_top:
#     vars:
#         X: seq[T1]
#         i: int
#     rewrite:
#         X[:i] ~> i..len(X) <<| X
