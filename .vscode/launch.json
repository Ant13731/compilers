{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceRoot}:~/GitHub/llvm-project/build/tools/mlir/python_packages/mlir_core"
            },
            // "cwd": "${fileDirname}",
            // "env": {
            //     "PYTHONPATH": "${workspaceFolder}${pathSeparator}${env:PYTHONPATH}"
            // }
        },
    ]
}